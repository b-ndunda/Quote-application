import { HighVoteCountDirective } from './high-vote-count.directive';

describe('HighVoteCountDirective', () => {
  it('should create an instance', () => {
    const directive = new HighVoteCountDirective();
    expect(directive).toBeTruthy();
  });
});
